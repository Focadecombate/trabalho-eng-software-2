import { Container, styled } from "@mui/material";

export const PageContainer = styled(Container)({
  width: "100vw",
  height: "90vh",
  display: "grid",
  placeItems: "center",
  textAlign: "center",
});
